<li class="cart__item">
  <% if product.image.attached? %>
    <%= image_tag(product.image, class: "cart__list-img") %>
  <% else %>
    <%= image_tag(product.thumbnail, class: "cart__list-img") %>
  <% end %>
  <div class="card__item-head">
    <h5><%= product.name %>
    </h5>
    <div class="cart__item-allprice">
      <span class="cart__item-price"><%= number_to_currency product.price %></span>
      <span class="cart__item-multiply">x</span>
      <span class="cart__item-quantity"><%=init_cart["#{product.id}"] %></span>
    </div>
  </div>
</li>
